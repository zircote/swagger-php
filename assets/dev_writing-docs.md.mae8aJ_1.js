import{_ as t,o,c as a,a5 as n}from"./chunks/framework.B1nibuwM.js";const u=JSON.parse('{"title":"Writing documentation","description":"","frontmatter":{},"headers":[],"relativePath":"dev/writing-docs.md","filePath":"dev/writing-docs.md"}'),i={name:"dev/writing-docs.md"};function s(r,e,h,d,l,c){return o(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="writing-documentation" tabindex="-1">Writing documentation <a class="header-anchor" href="#writing-documentation" aria-label="Permalink to &quot;Writing documentation&quot;">​</a></h1><p>Conventions for the hand-written pages under <code>docs/</code>. For which pages are generated and must not be edited, see <a href="/swagger-php/dev/docs-toolchain.html">Documentation toolchain</a>.</p><h2 id="state-a-fact-once" tabindex="-1">State a fact once <a class="header-anchor" href="#state-a-fact-once" aria-label="Permalink to &quot;State a fact once&quot;">​</a></h2><p>Do not restate something the page&#39;s own structure already establishes.</p><p>The beta status of the spec pipeline, for example, is carried by the <strong>Status</strong> row in <code>guide/modes.md</code>, by <code>(beta)</code> in the section headings, by the <code>::: warning Beta</code> callouts, by the version timeline, and by the 🧪 in the page titles. A sentence saying &quot;this is the only mode not marked beta&quot; adds nothing and rots independently of the places that do.</p><p>The test is not &quot;does this sentence read well&quot; but &quot;does the reader learn anything here they could not already see&quot;. This applies to maturity, version requirements, which namespace to use, and what a mode does.</p><p>Marketing filler and restatement filler are the same defect wearing different clothes. Replacing &quot;the stable, production-ready mode&quot; with &quot;the only mode not marked beta&quot; is not an improvement.</p><h2 id="detail-belongs-in-exactly-one-place" tabindex="-1">Detail belongs in exactly one place <a class="header-anchor" href="#detail-belongs-in-exactly-one-place" aria-label="Permalink to &quot;Detail belongs in exactly one place&quot;">​</a></h2><p>Orientation may appear in several places; detail may not.</p><p>It is fine — often necessary — for two pages to <em>introduce</em> the same subject, because they are read by different people arriving from different directions. What must not happen is the same specifics being spelled out twice, because the two copies drift and nothing reveals which one is stale.</p><p>The split that works is by depth, not by topic. A page aimed at users says what a thing does and why it matters, then links onward. The page aimed at contributors carries the mechanism, the rules, the exact lists. Neither repeats the other&#39;s half.</p><p><code>reference/architecture.md</code> and <code>dev/pipeline.md</code> are the worked example: the reference page describes the Assembler in a paragraph and links onward, while the internals page owns slot maps, the resolution algorithm and the root-attribute list. Before this split both described slot maps, augmenter ordering and the class hierarchy, in different words.</p><p>When you find yourself writing something a second time, that is the signal to link instead.</p><h2 id="reuse-the-content-do-not-copy-it" tabindex="-1">Reuse the content, do not copy it <a class="header-anchor" href="#reuse-the-content-do-not-copy-it" aria-label="Permalink to &quot;Reuse the content, do not copy it&quot;">​</a></h2><p>When the same text genuinely has to appear in more than one output, extract it and pull it in. Copying is what rots.</p><p>Two mechanisms are already in use here:</p><ul><li><code>docs/snippets/preamble_*.md</code> — markdown fragments the reference generators splice into their pages, via <code>DocGenerator::snippetContent()</code>. Editing the fragment updates the generated page.</li><li><code>&lt;&lt;&lt; @/snippets/path.php</code> — transcludes a real, test-executed source file into a page, so a documented example cannot drift from code that runs.</li></ul><p>A third, <code>&lt;!--@include: ./path.md--&gt;</code>, is supported by Vitepress but not yet used here. It suits prose shared between hand-written pages.</p><p>Prefer extracting a fragment over pasting, and prefer generating over both where the content is derivable at all — see <a href="/swagger-php/dev/docs-toolchain.html">the toolchain notes</a> for what is already generated.</p><h2 id="write-about-the-subject-not-about-the-documentation" tabindex="-1">Write about the subject, not about the documentation <a class="header-anchor" href="#write-about-the-subject-not-about-the-documentation" aria-label="Permalink to &quot;Write about the subject, not about the documentation&quot;">​</a></h2><p>A page describes its subject. Unless the documentation <em>is</em> the subject — as on this page, or the toolchain notes — it does not explain how the docs are produced or why a cross-reference can be trusted.</p><p>No page, of either kind, needs to announce its own audience or justify its existence. The title and the link that brought the reader here have already done that.</p><p>Three real examples, all from one page:</p><blockquote><p>The Augmenters reference — <strong>generated from the pipeline itself, so it cannot fall out of step with the code</strong></p></blockquote><blockquote><p>see Spec pipeline internals, <strong>which is written for people changing the pipeline rather than using it</strong></p></blockquote><blockquote><p><strong>This page stays at the level of &quot;what happens and in what order&quot;</strong></p></blockquote><p>Each is true, and each is invisible to the only question the reader has, which is how the thing works. Whether a reference is generated matters to whoever maintains it; the reader just follows the link.</p><p>The usual tell is a clause attached to a link, explaining the link. Let the link text do that work: &quot;see [Spec pipeline internals]&quot; is complete. Anything after the comma is the page talking about itself.</p><h2 id="enumerate-closed-sets-only" tabindex="-1">Enumerate closed sets only <a class="header-anchor" href="#enumerate-closed-sets-only" aria-label="Permalink to &quot;Enumerate closed sets only&quot;">​</a></h2><p>A list someone has to remember to update is a list that will be wrong.</p><p>Enumerate a set only when it is <strong>closed</strong> — defined somewhere in code, changing only when that code changes. The generated pages are closed: <code>tools/docgen.php</code> names them, so a table listing them holds until someone edits the generator. &quot;Everything else under <code>docs/</code>&quot; is open-ended; it grows whenever anyone adds a page, so it gets described rather than listed.</p><p>The same test applies to a count attached to a list. If the set can change, so can the number — which is why &quot;six pages&quot; goes along with the enumeration it summarised.</p><h2 id="do-not-hand-write-what-can-be-captured" tabindex="-1">Do not hand-write what can be captured <a class="header-anchor" href="#do-not-hand-write-what-can-be-captured" aria-label="Permalink to &quot;Do not hand-write what can be captured&quot;">​</a></h2><p>Command output, default configuration, and API listings should be copied from the real thing, not composed. A hand-written <code>openapi -h</code> block once carried the wrong format, a wrong <code>--mode</code> description, a <code>--version</code> default that did not exist, and four missing options — for as long as nobody ran it.</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./bin/openapi</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -h</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # capture this</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./bin/openapi</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> spec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -D</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src</span></span></code></pre></div><h2 id="do-not-cite-line-numbers" tabindex="-1">Do not cite line numbers <a class="header-anchor" href="#do-not-cite-line-numbers" aria-label="Permalink to &quot;Do not cite line numbers&quot;">​</a></h2><p><code>Class::method()</code> survives refactoring; <code>Foo.php:110</code> does not. Of three line references in ADR-001, two had already rotted.</p><h2 id="do-not-claim-what-you-have-not-verified" tabindex="-1">Do not claim what you have not verified <a class="header-anchor" href="#do-not-claim-what-you-have-not-verified" aria-label="Permalink to &quot;Do not claim what you have not verified&quot;">​</a></h2><p>If a page compares spec mode to classic mode, run both. Hedged comparisons — &quot;classic mode <em>may</em> emit the array form&quot; — are a sign the comparison was reasoned about rather than tested, and they are impossible for a reader to act on.</p><p>Either verify and state it plainly, or document only the behavior you know.</p><h2 id="do-not-contradict-the-page-you-are-on" tabindex="-1">Do not contradict the page you are on <a class="header-anchor" href="#do-not-contradict-the-page-you-are-on" aria-label="Permalink to &quot;Do not contradict the page you are on&quot;">​</a></h2><p>Two examples from the same page:</p><ul><li>attributes described as &quot;immutable&quot; while the augmenter section describes writing to them</li><li>&quot;all modes produce the same output&quot;, immediately above a table of differences between modes</li></ul><p>If a claim needs the reader to ignore the next paragraph, cut the claim.</p><h2 id="spelling" tabindex="-1">Spelling <a class="header-anchor" href="#spelling" aria-label="Permalink to &quot;Spelling&quot;">​</a></h2><p>The codebase is mixed. <code>normalise</code> sits alongside <code>serialization</code>; both spellings of <code>behavio(u)r</code> appear in <code>src/</code> and in <code>docs/</code>. There is no single convention to appeal to.</p><p>Match whatever the file you are editing already uses, and leave the rest alone. A bulk correction would churn far more than it fixes.</p><h2 id="structure" tabindex="-1">Structure <a class="header-anchor" href="#structure" aria-label="Permalink to &quot;Structure&quot;">​</a></h2><ul><li>Code snippets under <code>docs/snippets/</code> are executed by the test suite — see <a href="/swagger-php/dev/testing.html">Testing</a> for what a new snippet requires.</li><li>The <code>&lt;codeblock&gt;</code> component renders annotation / attribute / spec variants as tabs; keep the three implementations in sync.</li><li>Internal links use site-absolute paths (<code>/reference/architecture</code>), not relative file paths, so they survive being rendered at a different depth.</li></ul><h2 id="reviewing-documentation-changes" tabindex="-1">Reviewing documentation changes <a class="header-anchor" href="#reviewing-documentation-changes" aria-label="Permalink to &quot;Reviewing documentation changes&quot;">​</a></h2><p>A pass to run over a documentation diff — your own or someone else&#39;s. Every item below caught a real defect in this codebase at least once.</p><p>Where a check fails, fix the source rather than the symptom: a wrong generated page means a wrong docblock or generator, not a page to hand-edit.</p><h3 id="checks-you-can-run" tabindex="-1">Checks you can run <a class="header-anchor" href="#checks-you-can-run" aria-label="Permalink to &quot;Checks you can run&quot;">​</a></h3><ul><li>[ ] <strong>Generated pages untouched.</strong> <code>composer docs:gen</code> leaves no diff on the pages listed in <a href="/swagger-php/dev/docs-toolchain.html">the toolchain notes</a>.</li><li>[ ] <strong>Every <code>composer &lt;script&gt;</code> mentioned exists</strong> in <code>composer.json</code>.</li><li>[ ] <strong>Every code reference resolves.</strong> Class names, <code>Class::method()</code>, file paths, CLI flags, config keys — check each against the source. This catches the most damaging class of error, where confident prose describes an API that was renamed or never existed.</li><li>[ ] <strong>Command output was captured, not composed.</strong> Any <code>--help</code> text, default config dump or API listing should come from a real run.</li><li>[ ] <strong>No line-number citations.</strong> <code>Foo.php:123</code> rots silently; cite <code>Class::method()</code>.</li><li>[ ] <strong>Links resolve.</strong> Relative links point at files that exist; the site build fails on dead internal links, so <code>composer docs:build</code> covers the rest.</li><li>[ ] <strong>Section anchors verified, or dropped.</strong> A <code>#heading-slug</code> link is only safe when the heading is plain words — punctuation and dashes make the generated slug ambiguous. If you have not confirmed it, link to the page.</li><li>[ ] <strong>No volatile values.</strong> Counts (&quot;six pages&quot;, &quot;around 27 test files&quot;), ports, versions, anything that shifts without anyone editing the doc. Derive it, describe it qualitatively, or — where the tool announces it — say that instead. A dev server that prints its own URL should be documented as printing its URL, not as a port number.</li><li>[ ] <strong>Version and requirement claims match</strong> <code>composer.json</code>.</li><li>[ ] <strong>No marketing filler</strong> — &quot;production-ready&quot;, &quot;seamless&quot;, &quot;powerful&quot;, &quot;clean&quot;, &quot;simply&quot;, &quot;robust&quot;, &quot;comprehensive&quot;.</li><li>[ ] <strong>No stock phrase repeated</strong> across the diff. If the same formulation appears twice, one of them is padding.</li></ul><h3 id="checks-that-need-reading" tabindex="-1">Checks that need reading <a class="header-anchor" href="#checks-that-need-reading" aria-label="Permalink to &quot;Checks that need reading&quot;">​</a></h3><ul><li>[ ] <strong>No open-ended set enumerated.</strong> A list is for a set defined in code, which changes only when that code changes. Anything that grows when someone adds a file gets described instead.</li><li>[ ] <strong>Detail appears exactly once.</strong> Two pages may introduce the same subject; only one may carry the specifics. If you are writing something a second time, link instead.</li><li>[ ] <strong>Claims are verified, not reasoned.</strong> Hedges — &quot;may&quot;, &quot;should&quot;, &quot;is expected to&quot; — usually mark a claim nobody tested. Run it, then state it plainly, or cut it.</li><li>[ ] <strong>The page does not contradict itself.</strong> A claim the reader must ignore two paragraphs later is worse than no claim.</li><li>[ ] <strong>The level matches the audience.</strong> User-facing pages say what something does and link onward; contributor pages carry the mechanism. Neither should hold the other&#39;s half.</li><li>[ ] <strong>No commentary about the documentation itself</strong>, unless the docs are the page&#39;s subject — how a page is generated, why a link is trustworthy. Watch for a clause hanging off a link that explains the link.</li><li>[ ] <strong>No page announcing its own audience or purpose.</strong> &quot;This page is for…&quot;, &quot;these notes exist because…&quot;. Applies to contributor pages too, not just user-facing ones.</li><li>[ ] <strong>Nothing restates what the page&#39;s own structure already shows.</strong> A status table, a heading, or a callout already told the reader; a sentence repeating it only rots.</li><li>[ ] <strong>Spelling follows the file it is in</strong> — <code>docs/</code> is US, <code>src/</code> is British. Neither is worth correcting in bulk.</li><li>[ ] <strong>Shared content is reused, not copied</strong> — see <a href="#reuse-the-content-do-not-copy-it">above</a>.</li></ul>`,56)])])}const g=t(i,[["render",s]]);export{u as __pageData,g as default};
