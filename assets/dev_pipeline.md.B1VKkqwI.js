import{_ as s,o as t,c as i,a5 as a}from"./chunks/framework.B1nibuwM.js";const k=JSON.parse('{"title":"Spec pipeline internals","description":"","frontmatter":{},"headers":[],"relativePath":"dev/pipeline.md","filePath":"dev/pipeline.md"}'),n={name:"dev/pipeline.md"};function o(r,e,l,h,c,p){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="spec-pipeline-internals" tabindex="-1">Spec pipeline internals <a class="header-anchor" href="#spec-pipeline-internals" aria-label="Permalink to &quot;Spec pipeline internals&quot;">​</a></h1><p>The parts of the spec pipeline that are easiest to get wrong when reading the source. For how the pipeline fits together, see <a href="/swagger-php/reference/architecture.html">Architecture</a>.</p><h2 id="the-dtos-are-mutable" tabindex="-1">The DTOs are mutable <a class="header-anchor" href="#the-dtos-are-mutable" aria-label="Permalink to &quot;The DTOs are mutable&quot;">​</a></h2><p>Spec attributes are plain data containers, but they are <strong>not</strong> immutable, and neither is <code>Specification</code>. Augmenters assign to attribute properties throughout — <code>Refs::mergeAllOf()</code> nulls <code>$schema-&gt;properties</code>, <code>Types</code> fills schema fields in place — and <code>Specification</code> exposes public arrays that <code>add()</code> appends to.</p><p>What <em>is</em> true, and what distinguishes them from classic annotations, is that they carry no serialization logic. Serialization is the compiler&#39;s job.</p><h2 id="slot-maps-the-slot-belongs-to-the-parent" tabindex="-1">Slot maps: the slot belongs to the parent <a class="header-anchor" href="#slot-maps-the-slot-belongs-to-the-parent" aria-label="Permalink to &quot;Slot maps: the slot belongs to the parent&quot;">​</a></h2><p>Nesting is declared by the child, via two methods on <code>AttributeInterface</code>:</p><ul><li><code>merge()</code> — how this attribute composes into a <strong>sibling</strong> on the same reflector</li><li><code>contained()</code> — which <strong>outer-level</strong> attribute types can absorb it from an inner level</li></ul><p>Both return <code>[TargetClass =&gt; &#39;slot&#39;]</code>, and in both cases the slot names a property on the <strong>target</strong>, not on the attribute declaring it:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// OA\\Property</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> contained</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> array</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Schema</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::class</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;properties[]&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// \`properties\` lives on Schema</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A <code>[]</code> suffix appends to a collection; a bare name assigns a scalar. It is easy to read backwards.</p><p>Because the child declares the relationship, downstream code can extend the system: a custom attachable names its own nesting targets without touching any native attribute.</p><h3 id="how-resolution-runs" tabindex="-1">How resolution runs <a class="header-anchor" href="#how-resolution-runs" aria-label="Permalink to &quot;How resolution runs&quot;">​</a></h3><p>Resolution is driven purely by these declarations — it reads nothing from PHP&#39;s own structural semantics:</p><ol><li><strong>Sibling merge</strong> — attributes on the same reflector compose via <code>merge()</code></li><li><strong>Hierarchical absorb</strong> — what remains flows upward a level at a time via <code>contained()</code>, first match wins</li></ol><p>If a level has containers, an unmatched non-root attribute is an error. If a level has no containers at all, unmatched attributes pass through to the level above.</p><h3 id="what-survives-resolution" tabindex="-1">What survives resolution <a class="header-anchor" href="#what-survives-resolution" aria-label="Permalink to &quot;What survives resolution&quot;">​</a></h3><p>Only <em>root</em> attributes should remain, and those are what enters the Specification. A root attribute is one that can stand alone — it owns a bucket and needs no parent.</p><ul><li><strong>Always root</strong>: <code>Schema</code>, <code>Operation</code>, <code>PathItem</code>, <code>OpenApi</code>, <code>Info</code>, <code>Tag</code>, <code>Server</code>, <code>ExternalDocumentation</code>, <code>Security\\Scheme</code>, <code>Components</code>, <code>Attachable</code></li><li><strong>Conditionally root</strong>, when their own key is set and <code>ref</code> is not: <code>Response</code> (<code>response</code>), <code>Parameter</code> (<code>parameter</code>), <code>Link</code> (<code>link</code>); <code>RequestBody</code> needs <code>request</code> set but has no <code>ref</code> check</li><li><strong>Never root</strong>: <code>Header</code>, <code>Example</code>, <code>MediaType</code>, <code>Property</code> — these must nest inside a parent or sit in a <code>Components</code> container</li></ul><p>Each attribute decides for itself, in <code>isRoot()</code>.</p><p>The user-facing version of this distinction is in <a href="/swagger-php/guide/spec-attributes.html#components">Using Spec Attributes</a>; this list is the full one.</p><h2 id="directory-layout-is-not-the-class-hierarchy" tabindex="-1">Directory layout is not the class hierarchy <a class="header-anchor" href="#directory-layout-is-not-the-class-hierarchy" aria-label="Permalink to &quot;Directory layout is not the class hierarchy&quot;">​</a></h2><p><code>src/Spec/</code> nests directories for readability, not inheritance. Notably:</p><ul><li><code>Property extends AbstractAttribute</code> — <strong>not</strong> <code>Schema</code>. This is the deliberate change from classic (where <code>Annotations\\Property extends Schema</code>) and it is what makes stacking <code>#[OA\\Property]</code> and <code>#[OA\\Schema]</code> on the same target work.</li><li><code>Encoding extends AbstractAttribute</code> — <strong>not</strong> <code>MediaType</code>, despite <code>Property\\Encoded</code>.</li><li><code>Contact</code>, <code>License</code>, <code>ServerVariable</code> extend <code>AbstractAttribute</code>, not <code>Info</code>/<code>Server</code>.</li><li><code>OA\\Security</code> is a namespace, not a class. The classes are <code>Security\\Requirement</code> and <code>Security\\Scheme</code>.</li></ul><p>Genuinely nested: <code>Schema\\{AdditionalProperties,Items,Ref}</code>, <code>Property\\Encoded</code>, <code>Operation\\*</code>, <code>Parameter\\*</code>, <code>MediaType\\{Json,Xml}</code>, <code>Flow\\*</code>, <code>Security\\Scheme\\*</code>.</p><p>The <a href="/swagger-php/reference/spec-attributes.html">Spec Attributes reference</a> lists every attribute with its parameters and what it can nest into.</p><h2 id="reflectors-are-the-glue" tabindex="-1">Reflectors are the glue <a class="header-anchor" href="#reflectors-are-the-glue" aria-label="Permalink to &quot;Reflectors are the glue&quot;">​</a></h2><p>Every root DTO keeps the reflector it came from. This is how relationships that span buckets get resolved after assembly, without the DTOs having to reference each other:</p><ul><li><strong>PathItem to Operation</strong> — a PathItem sits on a class, operations on its methods; the <code>PathItems</code> augmenter uses <code>ReflectionMethod::getDeclaringClass()</code> to pair them</li><li><strong>Prefix composition</strong> — <code>ReflectionClass::getParentClass()</code> walks ancestors so parent PathItems can contribute path prefixes</li><li><strong>OperationId generation</strong> — class and method names come from the reflector</li><li><strong>Type inference</strong> — <code>Types</code> reads PHP type declarations off property and parameter reflectors</li></ul><p>This is what keeps the Assembler concerned only with nesting.</p><h2 id="normalise-on-input-store-the-simple-form" tabindex="-1">Normalise on input, store the simple form <a class="header-anchor" href="#normalise-on-input-store-the-simple-form" aria-label="Permalink to &quot;Normalise on input, store the simple form&quot;">​</a></h2><p>Where a property accepts both a rich input type and a plain serialized one, take both but convert immediately in the constructor. Properties always hold the simple form; enums, objects and convenience types are input sugar only.</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// OA\\Flow — FlowType accepted on input, stored as string</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> __construct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">|</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FlowType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">|</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $flow </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    parent::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__construct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: $x, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">attachables</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: $attachables);</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flow </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $flow </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">instanceof</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\BackedEnum</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $flow</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $flow;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Augmenters, compilers and serialization then never need to branch on type.</p><h2 id="augmenter-ordering-lives-in-one-place" tabindex="-1">Augmenter ordering lives in one place <a class="header-anchor" href="#augmenter-ordering-lives-in-one-place" aria-label="Permalink to &quot;Augmenter ordering lives in one place&quot;">​</a></h2><p>Registration order is <code>Builder::getDefaultAugmenters()</code>. The phase each augmenter runs in comes from its own <code>group()</code> — <code>Resolve</code> → <code>Reduce</code> → <code>Augment</code>. Within a phase, execution follows registration order.</p><p>Ordering that matters: <code>Inheritance</code> must run before <code>PathItems</code>, because inherited operations have to exist before path prefixes are resolved.</p>`,37)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
