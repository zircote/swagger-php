openapi: 3.0.0
info:
  title: API
  version: '1.0'
paths:
  /api/endpoint:
    get:
      responses:
        '200':
          description: 'successful operation'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PropertyInheritance'
components:
  schemas:
    PropertyInheritance:
      properties:
        inheritedfilter:
          type: string
      type: object
