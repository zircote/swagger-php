openapi: 3.2.0
info:
  title: 'Property Inheritance Scratch'
  version: '1.0'
paths:
  /api/endpoint:
    get:
      operationId: 'getInheritedFilters'
      responses:
        '200':
          description: 'successful operation'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PropertyInheritance'
components:
  schemas:
    PropertyInheritance:
      properties:
        inheritedfilter:
          type: string
      type: object
