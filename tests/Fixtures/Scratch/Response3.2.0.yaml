openapi: 3.2.0
info:
  title: Response
  version: '1.0'
paths:
  /endpoint/response-schema:
    post:
      operationId: 'responseSchema'
      responses:
        '200':
          description: 'All good'
          content:
            application/octet-stream:
              schema:
                type: string
                format: byte
