openapi: 3.2.0
info:
  title: 'List of types'
  version: '1.0'
paths:
  /api/endpoint:
    get:
      description: 'An endpoint'
      operationId: 'getTypes'
      responses:
        '200':
          description: OK
components:
  schemas:
    Types:
      properties:
        stringInteger:
          type:
            - string
            - integer
        massiveTypes:
          type:
            - string
            - number
            - integer
            - boolean
            - object
            - 'null'
      type: object
